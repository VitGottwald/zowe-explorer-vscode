import { fs } from "@zenfs/core";
export default fs;
// const { fs: fsZen } = require("@zenfs/core");

// const { WebStorage } = require("@zenfs/dom");

// configure({ backend: WebStorage, storage: localStorage });

// module.exports = fsZen;
